import ExpiredMessage from "./subscription-expired";
import TokenExpired from "./token-expired";

export const SubscriptionExpired = ExpiredMessage;
export const ResetPasswordExpired = TokenExpired;